# AGENTS.md — `agent_go_souschef`

Entry point for human contributors **and** AI agents working on this repository.

> **Rules tell you what. Patterns show you how.**

## What this repo is

`agent_go_souschef` is a single-binary CLI that builds a semantic symbol index
for Go repositories and exposes it to LLMs as a Model Context Protocol server.
Two integration paths:

- **MCP server** (`agent_go-souschef mcp`) — stdio MCP, four tools:
  `souschef_sync`, `souschef_query`, `souschef_source`, `souschef_changed`.
  Used by Claude Code, Codex, Cursor, or any MCP host.
- **Hook** (`agent_go-souschef hook install --claude`) — `PreToolUse` hook
  that lets Claude consult the index before reading files.

The index lives in `<workspace>/.repo-context/index.db` (SQLite, sqlc-typed).
Answers are compact (30–200 tokens); raw source is returned only via the
`souschef_source` tool.

Installation and Claude Code setup: see [`README.md`](README.md).

## Layout

| Path | Role |
|---|---|
| `cmd/agent_go-souschef/` | Binary entry point — parse args, build injector, dispatch. |
| `internal/bootstrap/` | remy wiring (`DoInjections`) + per-subcommand runners (`RunMCP`, `RunSync`). |
| `internal/index/goscan/` | Load Go packages, extract symbols + call graph (+ `symbols/` + `graph/` subpackages). |
| `internal/index/repomodel/` | Shared domain types + generic `Map`/`Filter`. |
| `internal/index/reposqlite/` | SQLite persistence (`reposqlite.go` + `_reads.go` + `_writes.go`); `sql/` is the sqlc source of truth; `db/` is generated. |
| `internal/source/gitprobe/` | go-git/v5 based change detection. |
| `internal/source/queryview/` | Compact result rendering. |
| `internal/integrations/mcpkit/` | Thin generic wrapper around the MCP SDK — `Tool[In, Out]`. |
| `internal/integrations/hooksetup/` | Hook install + `PreToolUse` handlers. |
| `pkg/repocontext/` | Public API — `*Service`, ports, `RegisterMCP`. |
| `test/fixtures/sample/` | Test fixtures (own `go.mod`, module `sample`). |
| `tools/` | Build/dev tooling (separate Go module, `Taskfile.yml`, `.golangci.yml`, tool directives). |
| `docs/rules/` | Mandatory engineering rules. |
| `docs/patterns/` | Canonical implementation recipes. |
| `docs/setup/` | End-user setup guides (native + Docker). |

## How to use the docs

1. **Start here**, then jump to [`docs/rules/`](docs/rules/) for mandatory rules.
2. Read [`docs/patterns/`](docs/patterns/) for canonical recipes with code.
3. For end-user installation, see [`docs/setup/`](docs/setup/) or the top-level [`README.md`](README.md).

## Canonical references

When a rule or pattern is ambiguous, fall back — in order — to:

1. **[Effective Go](https://go.dev/doc/effective_go)** and
   **[Go Code Review Comments](https://go.dev/wiki/CodeReviewComments)** — baseline language style.
2. **`tools/.golangci.yml`** — the enforceable subset. Formatters are `gofumpt` + `goimports` + `golines`.
3. **`docs/patterns/`** — fully-worked Go examples. If reality drifts from them, update the
   pattern in the same PR.

## How to run things

```sh
# Build & smoke
go build ./cmd/agent_go-souschef
./agent_go-souschef sync                    # build index in cwd
./agent_go-souschef mcp                     # start MCP server (Ctrl-C to exit)

# Hook setup
./agent_go-souschef hook install --claude

# Codegen / lint / modernize (run from repo root)
go tool -modfile=tools/go.mod sqlc generate
go tool -modfile=tools/go.mod modernize -fix ./...

# Via Taskfile (from tools/)
task -d tools gen:code
task -d tools gen:modernize
task -d tools run:checks
task -d tools run:formatters

# Tests
go test ./...
```

## Stop signs

These are not negotiable:

- ❌ **No global mutable state.** No package-level vars used as caches, clients, or singletons.
- ❌ **No side effects in `init()` or at package load.** Connections happen inside
  `bootstrap.DoInjections`, not at import time.
- ❌ **No raw SQL strings in Go files.** All queries live in
  `internal/index/reposqlite/sql/queries.sql` and are generated by sqlc.
- ❌ **No `panic` outside `main()`.** Internal packages return errors.
- ❌ **No silent error swallowing.** Wrap with `fmt.Errorf("...: %w", err)`.
- ❌ **No imports of `internal/index/reposqlite` concrete types from `pkg/repocontext/`.**
  Depend on the `SymbolStore` interface declared in `pkg/repocontext/contracts.go`.
- ❌ **No edits to `internal/index/reposqlite/db/`.** It is sqlc-generated.
- ❌ **No shelling out** to `git`, `grep`, `find`, or any external CLI when
  stdlib / `golang.org/x/tools` / `go-git` already covers it.
- ❌ **No single-file packages.** A package always has ≥2 files plus tests.
- ❌ **Hard cap 150 lines per file.** Past that, split by concern.

## For AI agents specifically

- Start every change by reading the rules referenced by the task.
- Prefer surgical edits. Don't reformat unrelated files.
- When adding a new MCP-exposed operation:
  1. Add a method to `*repocontext.Service` (`pkg/repocontext/<op>.go`).
  2. Add SQL to `internal/index/reposqlite/sql/queries.sql` if needed and
     run `sqlc generate`.
  3. Register the new tool with `mcpkit.Tool(...)` in
     `pkg/repocontext/mcptools.go`.
  4. If it needs CLI surface, add a `Run<Op>` to `internal/bootstrap/runner.go`
     and a `case` in `cmd/agent_go-souschef/main.go`.
- Consumer-defined interfaces: `SymbolStore` is declared in `pkg/repocontext/`,
  not in `internal/index/reposqlite/`. Follow this pattern for any new port.
- The `test/fixtures/sample/` directory has its own `go.mod` (module `sample`).
  Do not add `//go:build ignore` to fixture files — the separate module is sufficient.
